'use strict';

describe('myApp.playersView module', function() {

  beforeEach(module('myApp.playersView'));

  describe('playersView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var playersCtrl = $controller('PlayersCtrl');
      expect(playersCtrl).toBeDefined();
    }));

  });
});